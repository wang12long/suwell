const getters = {
  // getCount: state => state.count,
  getMenus : state => state.menus,
  getisLoading : state => state.isLoading,
  getisShowSecondMenu : state => state.showSecondMenu,
  getPreSelect : state => state.preSelect
}
export default getters
